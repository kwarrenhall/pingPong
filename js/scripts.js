
var pingPong = function(myNumber) {

   for (var i = 1; i <= myNumber; i++) {
     if (i % 15 === 0) {
     myNumber = ""
     myNumber += "pingPong"
     return myNumber;
   }
};
};

 // event.preventDefault();
