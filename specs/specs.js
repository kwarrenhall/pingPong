
describe("countUp", function(){
it("will count from one to five", function() {
    expect(countUp(5)).to.eql([1,2,3,4,5]);
  });
it("will count from one to a user specified number", function() {
    expect(countUp(number)).to.eql(counter)
});
});
